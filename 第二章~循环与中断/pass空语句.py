# pass是python中的保留字，在语法结构中只起到占位符作用，使语法结构完整，不报错，一般可用于if，for，while，函数定义，类的定义中
#示例：
if True:
    pass  #用pass占位，没有的话语法会报错！
while True:
    pass   #用pass占位，没有的话语法会报错！
for i in range(10):
    pass   # 用pass占位，没有的话语法会报错！

